export type AccountSummary = {
  id: number;
  name: string;
};

export type Client = {
  id: string;
  name: string;
};

export type ClientDto = {
  name: string;
};

export type Item = {
  id: string;
  name: string;
  price: number;
};

export type ItemDto = {
  name: string;
  price: number;
};
